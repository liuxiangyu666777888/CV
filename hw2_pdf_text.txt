===== PAGE 1 =====
期中作业
任务 1：微调在 ImageNet 上预训练的卷积神经网络实现宠物识别
基本要求：
（1） 数据集与 Baseline：在 Oxford-IIIT Pet Dataset 数据集上，修改现有的 CNN
架构（ResNet-18 或 ResNet-34）的输出层作为 Baseline 进行训练和测试，训
练时使用在 ImageNet 上预训练得到的网络参数进行初始化，从零开始训练新
的输出层，并对其余参数使用较小的学习率进行微调；
（2） 超参数分析：观察不同的超参数，如训练步数、学习率，及其不同组合带来的
影响，并尽可能提升模型性能；
（3） 预训练消融实验：与仅使用 Oxford-IIIT Pet Dataset 数据集从随机初始化的网
络参数开始训练得到的结果进行对比，观察预训练带来的提升；
（4） 引入注意力机制：在 Baseline 模型的基础上，手动修改网络结构，加入注意力
模块（如 SE-block, CBAM），或者直接使用轻量级的 Vision Transformer (如
ViT-Tiny，Swin-T)，进行训练并与 Baseline 对比模型性能（Accuracy）。
任务 2：场景目标检测与视频多目标跟踪
基本要求：
（1） 使用 VisDrone 无人机航拍检测数据集，微调训练 YOLOv8 或同类现代单阶段
模型，得到你的专属检测模型。
（2） 视频流检测与多目标跟踪：准备一段时长 10-30 秒的测试视频（可以是自己手
机拍摄的校园/路口视频等）。利用你训练好的模型，结合多目标跟踪算法（如
YOLOv8内置的tracking），对视频进行逐帧推理。要求不仅输出BoundingBox
与类别，还必须为每个同一目标分配稳定的跟踪 ID（Tracking ID）。
（3） 遮挡与 ID 跳变分析：多目标跟踪中最具挑战性的问题是物体遮挡。请在你的
测试视频中，挑选一个发生目标遮挡或密集交汇的片段，截取连续的 3-4 帧画
===== PAGE 2 =====
面进行可视化展示。结合课上所学理论，详细分析跟踪算法在面对此场景时，是
成功维持了原有 ID，还是发生了目标丢失/ID 跳变，并简述其原理或原因。
（4） 越线计数：在视频画面中设定一条虚拟的线，利用检测框中心的坐标与Tracking
ID 的连续性，编写简单的逻辑判断，统计并显示视频播放期间“跨越该线的物
体总数”。
任务 3：从零搭建与损失函数工程：图像分割模型的像素级训练
基本要求：
（1） 网络搭建：请勿使用任何预训练权重，使用深度学习框架的基础 API，从零开
始手写搭建一个经典的语义分割网络 U-Net，必须包含完整的下采样编码器、
上采样解码器以及特征拼接 Skip Connection 的逻辑。
（2） 轻量级数据集训练：为了在无预训练的情况下保证收敛速度，复用轻量级的三
分类分割数据集 Oxford-IIIT Pet Dataset。使用随机初始化的权重开始训练。
（3） 损失函数工程：图像分割任务常面临严重的前景/背景像素不平衡问题。请手动
编写代码实现 Dice Loss。分别使用以下三种损失配置训练模型，并对比它们在
验证集上的 mIoU 表现：
• 仅使用标准的交叉熵损失（Cross-Entropy Loss）
• 仅使用手动实现的 Dice Loss
• 组合损失（Cross-Entropy Loss + Dice Loss）
提交要求：
（1） 实验报告：仅提交 PDF 格式的实验报告。报告内必须包含：
• 对模型结构、数据集和实验结果的基本介绍；
• 详细的实验设置：训练测试集划分、网络结构、batch size、learning rate、
优化器、iteration、epoch、loss function、评价指标等；
• 利用 wandb 或 swanlab 可视化截图：训练过程中在训练集和验证集上的
loss 曲线，以及验证集上的 Accuracy / mAP 曲线。
（2） 代码与模型：
• 代码提交到自己的 public Github repo，repo 的 README.md 文件中应详细、
清晰地指明如何配置环境、进行训练和测试；
2
===== PAGE 3 =====
• 可以使用 PyTorch 或 TensorFlow 等 Python 深度学习框架；
• 训练好的模型权重上传到百度云/Google Drive 等网盘；
• 注意：实验报告内必须包含实验代码所在的 Github repo 链接及模型权
重的网盘下载地址。
注意与纪律说明：
• 本次作业为组队作业，组队人数少于或等于 2 人。
• 同等质量工作下，1 人完成有额外加分。
• 实验报告小组成员中有一人提交即可，报告首页务必标明所有组员的姓名、学
号以及具体分工情况。
3